import { StyleSheet } from 'react-native';

export const ph = 24
export const pt = 12

export const globalStyle = StyleSheet.create({
  padding: {
    paddingHorizontal: ph,
    paddingTop: pt,
  },
  paddingHorizontal: {
    paddingHorizontal: ph,
  },
  paddingTop: {
    paddingTop: pt,
  },
});
